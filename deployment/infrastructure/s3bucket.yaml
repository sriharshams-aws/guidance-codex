AWSTemplateFormatVersion: '2010-09-09'
Description: 'S3 Bucket for CloudFormation artifacts (Lambda code packages)'

Resources:
  # S3 Bucket for CloudFormation artifacts (Lambda code packages)
  CfnArtifactsBucket:
    Type: AWS::S3::Bucket
    Metadata:
      checkov:
        skip:
          - id: CKV_AWS_18
            comment: >-
              Accepted risk: Artifacts bucket logs to itself; a dedicated cross-stack
              logging bucket is not deployed with this stack.
      cfn_nag:
        rules_to_suppress:
          - id: W35
            reason: >-
              Accepted risk: Artifacts bucket logs to itself; a dedicated cross-stack
              logging bucket is not deployed with this stack.
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      VersioningConfiguration:
        Status: Enabled
      ObjectLockEnabled: true
      ObjectLockConfiguration:
        ObjectLockEnabled: Enabled
        Rule:
          DefaultRetention:
            Mode: GOVERNANCE
            Days: 1
      AccessControl: Private
      # NOTE: This bucket logs access to itself (access-logs/ prefix). For production use,
      # consider deploying a dedicated logging bucket to achieve tamper-evident, separation-of-
      # concerns logging. Self-logging provides basic audit visibility but a malicious actor
      # with bucket access could delete both the data and its access logs simultaneously.
      LoggingConfiguration:
        LogFilePrefix: access-logs/
      LifecycleConfiguration:
        Rules:
          - Id: DeleteOldVersions
            Status: Enabled
            NoncurrentVersionExpirationInDays: 30
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      Tags:
        - Key: Name
          Value: codex-cfn-artifacts
        - Key: Purpose
          Value: CloudFormation Lambda packaging

Outputs:
  CfnArtifactsBucket:
    Description: S3 bucket for CloudFormation artifacts
    Value: !Ref CfnArtifactsBucket
    Export:
      Name: !Sub "${AWS::StackName}-CfnArtifactsBucket"
